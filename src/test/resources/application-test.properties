# Tests run against a throwaway in-memory database so they never touch the
# file-mode database used for the demonstration screenshots.
spring.datasource.url=jdbc:h2:mem:bookwise-test;DB_CLOSE_DELAY=-1
spring.jpa.hibernate.ddl-auto=create-drop
spring.h2.console.enabled=false
spring.jpa.open-in-view=false
